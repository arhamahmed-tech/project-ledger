# Original specs / PRDs

Place user-provided product specs, PRDs, and ProductSpec drafts here.

Agents: read these before `ledger new spec` or `ledger revise SPEC-*`.
Do not overwrite files in this folder. Derive into `docs/product/specs/SPEC-####/`.
