---
description: Use when choosing skills, MCP tools, or hook-driven workflows — agent toolkit order
alwaysApply: false
---

# Agent toolkit

Follow `docs/agent-protocol.md` decision order:

**skill → /find-skills → MCP → hooks → implement**

Project skills live in `.cursor/skills/` (includes find-skills). Works with `AGENTS.md` for all harnesses.
