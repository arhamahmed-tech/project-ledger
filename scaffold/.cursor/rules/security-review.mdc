---
description: Use for auth, payments, PII, admin, uploads, webhooks, secrets, or risky dependency changes
alwaysApply: false
---

# Security & code review

See `docs/agent-protocol.md` § Security.

Required for auth, payments, PII, admin, uploads, webhooks, secrets, risky deps.
Prefer `/review-security` or `/review-bugbot` when available. Record `EVD-*` review evidence for ledger work.
