# Cursor hooks

| Event | Script |
|-------|--------|
| `beforeSubmitPrompt` | toolkit reminder (skills / MCP / style / security) |
| `stop` | validate ledger; follow-up only on failure (`loop_limit: 1`) |
