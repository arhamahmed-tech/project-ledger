# Toolkit — skills, MCP, hooks

Follow `docs/agent-protocol.md` decision order:

skill → find-skills → MCP → hooks → implement.

Do not skip `/find-skills` when inventing a multi-step workflow that likely exists as a skill.
