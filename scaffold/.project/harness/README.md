# Shared harness hooks

Used by:

- Cursor → `.cursor/hooks.json`
- Claude Code → `.claude/settings.json`

Scripts:

| Script | Purpose |
|--------|---------|
| `toolkit-reminder.sh` | Remind skills / MCP / security / style |
| `validate-on-stop.sh` | Run `project-ledger validate`; warn on failure |
